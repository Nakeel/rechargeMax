class BuildConfig {
  final String baseUrl;
  final String appName;
  final String? paystackKey;
  final String? mapApiKey;

  BuildConfig({required this.baseUrl, required this.appName, this.paystackKey, this.mapApiKey}) ;
}
